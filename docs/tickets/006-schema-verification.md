# Schema Verification

Verify the EHR schema is correctly created in Kuzu. Test basic CRUD operations -- create a test patient, add conditions/medications/labs, query relationships, and verify FTS search works.
