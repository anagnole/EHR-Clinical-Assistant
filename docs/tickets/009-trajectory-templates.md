# Trajectory Templates

Implement 8 trajectory templates that generate patients with specific clinical patterns: diabetic-a1c (30 patients), heart-failure (25), drug-interaction (20), hypertension (30), ckd-progression (20), polypharmacy (20), pregnancy (15), routine-healthy (40). Each template implements the TrajectoryTemplate interface with generate() and groundTruth() methods.
