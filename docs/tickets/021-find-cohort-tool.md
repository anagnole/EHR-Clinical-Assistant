# Find Cohort Tool

Build the find_cohort MCP tool. Multi-hop intersection query that finds patients matching multiple criteria simultaneously: conditions, medications, lab criteria (code + min/max), age range, gender. Returns matching patient count and list with match strength.
