# Create EHR Instance

Create the ThesisBrainifai Brainifai instance via `brainifai init --template ehr` inside the project directory. This registers the project as a child node of the global Brainifai instance with its own Kuzu database.
