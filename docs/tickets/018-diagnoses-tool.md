# Diagnoses Tool

Build the get_diagnoses MCP tool. Returns a patient's condition list with status filter (active/resolved/all). Includes onset date, resolved date, and ICD-10 code. Returns record_ids per condition.
