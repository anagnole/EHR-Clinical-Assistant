# Medications Tool

Build the get_medications MCP tool. Returns a patient's medication list with filters for status (active/stopped/all) and since date. Includes prescribing provider if available. Returns record_ids per medication.
