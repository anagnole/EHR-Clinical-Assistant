# Scorers

Build the scoring system: F1 scorer (precision/recall for set answers), exact match scorer, MAE scorer (mean absolute error for numeric answers), accuracy scorer (for boolean answers), and hallucination detector (LLM-as-judge that checks claims against cited record_ids).
