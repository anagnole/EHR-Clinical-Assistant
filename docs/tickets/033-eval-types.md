# Evaluation Types

Define TypeScript types for the evaluation system: EvalQuestion (question text, type, expected answer, answer type, patient_id, record_ids), RunResult (answer, citations, latency, tool calls, token usage), EvalResult (scored comparison).
