# Data Ingestion

Ingest all generated patient data into the EHR Kuzu instance using MERGE-based upserts. Upsert all 7 node types and 12 relationship types. Verify counts match expected totals (200 patients, proportional encounters/conditions/etc.).
