# SQL Adapter

Build the SQL adapter with 7 retrieval functions equivalent to the graph MCP tools. Uses standard SQL JOINs and WHERE clauses for filtering. Temporal and cohort queries require multi-table JOINs with subqueries.
