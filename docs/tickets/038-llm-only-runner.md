# LLM-Only Runner

Build the LLM-only evaluation runner. Calls Claude via Anthropic API with no tools -- only the patient record prompt from the prompt builder. Tests raw LLM capability without retrieval.
