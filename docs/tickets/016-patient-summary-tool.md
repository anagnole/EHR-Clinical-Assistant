# Patient Summary Tool

Build the get_patient_summary MCP tool. Graph traversal from Patient to active conditions, active medications, and recent encounters (last 5). Returns structured summary with record_ids for all referenced records.
