# Seeded PRNG

Build a deterministic PRNG utility (mulberry32 or similar) that produces identical data from the same seed. This ensures reproducible data generation -- same seed always equals same patients, same ground truth.
