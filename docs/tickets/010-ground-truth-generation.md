# Ground Truth Generation

Each trajectory template produces 3-6 ground truth questions per patient with exact answers, answer types (set, exact, numeric, boolean, free_text), and supporting record_ids. Collect all ground truth entries and write to data/generated/ground-truth.json.
