# Prompt Builder

Build the prompt builder that serializes a patient's full record into structured text for the LLM-only baseline. Include demographics, active conditions, full medication list, recent labs, encounter history, and procedures. Cap at ~8000 tokens.
